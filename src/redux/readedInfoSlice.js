// import { createSlice } from '@reduxjs/toolkit';

// const initialState = {
//     pages: [],
// };

// export const readedInfoSlice = createSlice({
//     name: 'pages',
//     initialState,
//     reducers: {
//         setPageNumber: (state, action) => {
//             state.products = action.payload;
//         }
//     },
// });

// export const { setPageNumber } = readedInfoSlice.actions;

// export default readedInfoSlice.reducer;